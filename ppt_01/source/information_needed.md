# 需要你提供或填写的信息

这个文件是我们后续协作的“信息收集表”。你可以直接在每一项下面填写内容，也可以把相关文件放到指定目录后在这里说明文件名。

## 1. 原 PPT 的标准渲染结果

为了让 LaTeX 版本尽量复现原 PPT，需要知道你认为“正确”的原始视觉效果。

请尽量提供：

- 从 PowerPoint 或 WPS 导出的 PDF。
- 每一页导出的 PNG 图片，建议 1920x1080 或更高。

建议放置位置：

```text
ppt_01/source/reference/
```

请填写：

```text
原始 PDF 文件名：
逐页 PNG 文件名规则：例如 slide_01.png, slide_02.png, ...
导出软件：PowerPoint / WPS / Keynote / 其他
导出系统：Windows / macOS / Linux / 其他
```

你的填写：

```text
百态冬韵.pdf
”百态冬韵“文件夹，幻灯片1.pdf，幻灯片2.pdf
WPS
arch linux
```

## 2. 字体信息

PPT 的字体会强烈影响复刻效果。请打开原 PPT，查看主要文字使用了什么字体。

请尽量填写：

```text
中文标题字体：
中文正文字体：
英文字体：
数字字体：
是否有特殊字体文件：
```

如果你有字体文件，可以放到：

```text
ppt_01/source/fonts/
```

你的填写：

```text
中文标题字体：微软雅黑
中文正文字体：微软雅黑 Light
英文字体：segoe UI Black
数字字体：Segoe UI Black
是否有特殊字体文件：不知道
```

## 3. Logo 与学校标识

请说明模板里是否必须使用同济大学校徽、校名、学院名或课题组标识。

请填写：

```text
是否需要同济大学校徽：
是否需要同济大学中文校名：
是否需要英文校名：
学院或单位名称：
是否有单独 logo 文件：
```

如果有 logo 或矢量文件，建议放到：

```text
ppt_01/source/assets/
```

你的填写：

```text
看起来都是不需要的
```

## 4. 模板用途

不同用途会影响页面命令的封装方式。

请填写：

```text
主要用途：课程汇报 / 开题答辩 / 毕设答辩 / 组会 / 学术报告 / 其他
预计页数：
是否需要中英文混排：
是否经常放公式：
是否经常放代码：
是否经常放表格：
是否经常放大图：
```

你的填写：

```text
主要用途：科研汇报
预计页数：原模板 16 也
是否需要中英文混排：需要
是否经常放公式：需要
是否经常放代码：需要
是否经常放表格：需要
是否经常放大图：需要
```

## 5. 复刻优先级

请选择更偏向哪一种。

```text
视觉一致性优先：更像原 PPT，但部分装饰可能作为图片背景。
可编辑性优先：尽量用 LaTeX 重画，但可能和原 PPT 有细微差异。
折中：重要文字和内容可编辑，复杂背景和装饰用图片。
```

你的选择：

```text
折中
```

## 6. 需要优先实现的页面

我建议优先实现第 1、2、3、16 页，然后再做中间内容页。

如果你有不同优先级，请填写：

```text
第一批要实现的页码：
第二批要实现的页码：
最重要的一页：
可以暂时不做的一页：
```

你的填写：

```text
就按照你的
```

## 7. 可接受的差异

有些 PPT 效果在 LaTeX 中可以复刻，但成本较高。请说明哪些地方可以接受略有差异。

请填写：

```text
颜色是否必须完全一致：
字体是否必须完全一致：
图片裁剪是否必须完全一致：
阴影和透明度是否必须完全一致：
动画是否需要复刻：
```

你的填写：

```text
颜色是否必须完全一致：是
字体是否必须完全一致：是
图片裁剪是否必须完全一致：是
阴影和透明度是否必须完全一致：可以有些偏差
动画是否需要复刻：不太需要动画，因为是汇报模板
```

## 8. 输出形式

请填写你希望最终拿到什么。

```text
只需要一个 main.tex：
需要独立 .sty 主题文件：
需要示例 demo：
需要中文使用说明：
需要 Makefile 或编译脚本：
希望使用 latexmk / xelatex / tectonic / 其他：
```

你的填写：

```text
我不太懂，按照你的推荐即可
```

## 9. 编译环境

请填写你本机或目标环境的 LaTeX 情况。

```text
操作系统：
LaTeX 发行版：TeX Live / MiKTeX / MacTeX / TinyTeX / 不清楚
是否可以使用 XeLaTeX：
是否可以安装缺失宏包：
常用编辑器：VS Code / TeXstudio / Overleaf / 其他
```

你的填写：

```text
估计还没有 latex 环境，你可以指导一下我
```

## 10. 内容替换需求

如果你希望这个模板直接变成某个真实汇报，而不只是模板，请填写默认内容。

```text
报告标题：
副标题：
汇报人：
导师：
学院/单位：
日期：
目录条目：
```

你的填写：

```text
不，就是模板
```

## 11. 每轮反馈记录

后续我生成 LaTeX PDF 后，你可以在这里写反馈。建议格式如下：

```text
第 1 页：
- 标题需要更靠上。
- 背景图片颜色偏暗。

第 2 页：
- 目录数字太小。
- 右侧装饰线条不够像。
```

你的反馈：

```text
ok
```

